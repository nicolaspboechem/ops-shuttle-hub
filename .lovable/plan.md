

# Atualizar versao para 1.9.0

Atualizar o arquivo de versao do aplicativo para refletir as melhorias implementadas (sincronizacao de missoes, checkout bidirecional, badges visuais).

## Alteracao

**Arquivo:** `src/lib/version.ts`
- `APP_VERSION`: de `'1.8.1'` para `'1.9.0'`
- `APP_BUILD_DATE`: atualizar para `'2026-02-14'` (data atual)

Incremento MINOR (1.8 -> 1.9) justificado pelas novas funcionalidades adicionadas sem quebra de compatibilidade.

